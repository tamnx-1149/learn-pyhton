# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Django-specific files
*.log
*.pot
local_settings.py  # Or any other file containing local/sensitive settings
db.sqlite3         # If using SQLite as the database backend
media/             # Directory for uploaded user media files
static/            # Directory for collected static files (after collectstatic)

# Virtual Environment
.env/
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE-specific files (e.g., PyCharm, VS Code)
.idea/
.vscode/

# Operating System files
.DS_Store
Thumbs.db

# Test coverage and build artifacts
.coverage
.tox/
.eggs/
dist/
build/
*.egg-info/

# Pipenv files (if used)
Pipfile.lock  # Recommended to include, but can be ignored if desired

# Poetry files (if used)
poetry.lock   # Recommended to include, but can be ignored if desired
.env
